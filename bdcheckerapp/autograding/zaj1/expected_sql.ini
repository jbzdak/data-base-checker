[DEFAULT]

t3=SELECT wind_speed * cos(radians(wind_dir)) as wind_x,  wind_speed * sin(radians(wind_dir)) as wind_y  from zaj1 ORDER BY date;
t4=SELECT pm_10 from zaj1 where wind_speed > 1 AND (ozon = 0 OR ozon is null) ORDER BY date;
t5=SELECT pm_10 from zaj1 ORDER by date desc;